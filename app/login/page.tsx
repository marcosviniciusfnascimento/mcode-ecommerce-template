import LoginForm from "@/components/form/LoginForm";

export default function LoginPage() {
  return (
    <div className="w-full h-full min-h-screen flex items-center justify-center flex-col bg-gradient-to-br from-neutral-100 via-stone-200 to-stone-400">
      <LoginForm />
    </div>
  );
}
